class Music {
    constructor({ name, id, duration, artistName, picUrl, url }) {
        this.name = name
        this.id = id
        this.duration = duration
        this.artistName = artistName
        this.picUrl = picUrl
        this.url = url
    }
}

export default Music